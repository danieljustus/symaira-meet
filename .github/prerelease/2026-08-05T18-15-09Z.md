<!-- prerelease: timestamp=2026-08-05T18:15:09Z repo=danieljustus/symaira-meet head=0593ccada2df9e92f73b30c11487b1a2ddedcfd1 target=v0.3.1 bump=patch gate=pass -->

# Prerelease Gate — 2026-08-05

## Summary
- Repo: danieljustus/symaira-meet
- HEAD: 0593cca (test(core): cover post-recording pipeline phases)
- Last release: v0.3.0 (2026-08-02)
- Proposed target: v0.3.1 (bump: patch)
- Gate: pass

## Gate checks
| Check | Result | Detail |
|---|---|---|
| CI / tests (HEAD) | PASS | CI run 31033210098 (workflow CI, push@main) conclusion=success; runs `make build`, `make coverage` (55% floor), `make lint` |
| Version consistency | PASS | 0 semver mentions in README/docs (versionless by design); no static version file (version embedded at release-build time from tag via scripts/build-release.sh + EmbeddedRelease.swift); 0 fixes needed |
| README / doc currency | PASS | Homebrew install section (#119) correct: `brew install danieljustus/tap/symmeet` + `--cask .../symmeet-agent`; CLI command surface matches README; badges/links valid |
| Coverage vs threshold | PASS | CI hard gate COVERAGE_FLOOR=55 (scripts/coverage-report.sh) enforced in CI and green on this HEAD (hard repo gate, 55%) |
| Coverage regression | PASS | Cycle_08 measurement on this HEAD: +2.7 pp vs immediate pre-change, +12.3 pp vs audit baseline (Jul-22 61.0%); no drop vs last release (v0.3.0) |
| Patch coverage | PASS | n/a — HEAD == origin/main, no unreleased diff vs default branch |
| Assets preview | PASS | release.yml builds/attaches symmeet_*.tar.gz, SymMeetAgent_*.dmg, checksums.txt, sbom.spdx.json, *_notices.tar.gz; verified by scripts/release-smoke.sh + codesign/spctl/stapler checks |
| Distribution publishers | PASS | Formula/symmeet.rb (CLI) + Casks/symmeet-agent.rb (GUI) both exist in danieljustus/homebrew-tap @ v0.3.0; release.yml writes both (sed + heredoc, correct paths); HOMEBREW_TAP_GITHUB_TOKEN set at repo level; no skip flags |
| Security | PASS | 0 open code-scanning alerts; 0 open dependabot alerts; codeql.yml workflow present |

## Findings
- none (no BLOCK, no WARN findings that affect this gate)

## Notes
- **Milestone vs tag:** 0 open milestones. Closed milestones v0.1.0/v0.2.0/v0.3.0 match released tags; v0.4.0-beta.1 and v0.1.1/v0.2.1 are closed legacy titles. No open milestone conflicts with target v0.3.1; 07-gh-release milestone reconciliation (fallback create/assign) applies if a milestone is wanted.
- **Local checks:** `make build` passed (359/359), `make lint` passed (pre-existing warnings only, exit 0). `swift test` not runnable on this machine (CommandLineTools-only, no XCTest — documented in README); CI is the canonical test gate and is green.
- **Coverage evidence:** a fresh local `make coverage` re-measurement was not re-run — CI already enforces the 55% floor on this exact HEAD and passed, and Cycle_08 measured coverage on the same HEAD with positive deltas vs both baselines minutes before this gate. The 2026-08-05T09:05 report (head=e8205c1, 62.8%) predates HEAD and the #123 floor gate, so it is superseded.
- **Bump basis:** since v0.3.0 — one user-facing fix (#121 JSON error envelopes under --json, process --speakers code), one internal dead-code removal/refactor (#122, no contract impact), remaining commits are tests/CI/docs → patch, no major/minor trigger.

## Applied mechanical fixes (default mode)
- (— none —)

## Next step
- gate=pass → /07-gh-release
